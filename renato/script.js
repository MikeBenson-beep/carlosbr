const header=document.getElementById("header"),menuButton=document.querySelector(".menu-toggle"),form=document.getElementById("contact-form"),note=document.getElementById("form-note");
document.getElementById("year").textContent=String(new Date().getFullYear());
menuButton.addEventListener("click",()=>{const open=header.classList.toggle("open");document.body.classList.toggle("menu-open",open);menuButton.setAttribute("aria-expanded",String(open));menuButton.setAttribute("aria-label",open?"Cerrar menú":"Abrir menú")});
document.querySelectorAll("#nav a").forEach(link=>link.addEventListener("click",()=>{header.classList.remove("open");document.body.classList.remove("menu-open");menuButton.setAttribute("aria-expanded","false")}));
document.querySelectorAll("[data-service]").forEach(link=>link.addEventListener("click",()=>{const value=link.dataset.service;document.getElementById("service").value=value.includes("Diagnóstico")?"Diagnóstico de cartera":value.includes("Gestión")?"Gestión de recuperación":value.includes("Asesoría")?"Asesoría para administración":"Quiero conocer el servicio"}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(element=>observer.observe(element));
form.addEventListener("submit",event=>{event.preventDefault();const data=new FormData(form);const message=["Hola, quiero consultar por la recuperación de cartera de mi conjunto o edificio.",`Nombre: ${data.get("name")}`,`Conjunto o edificio: ${data.get("building")}`,`Ciudad: ${data.get("city")}`,`Necesidad: ${data.get("service")}`,`Detalle: ${data.get("message")||"Por confirmar"}`].join("\n");note.textContent="Solicitud lista. Abriendo WhatsApp para compartirla…";window.open(`https://wa.me/593998032770?text=${encodeURIComponent(message)}`,"_blank","noopener,noreferrer")});
